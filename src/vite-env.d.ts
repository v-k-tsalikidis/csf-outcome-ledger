/// <reference types="vite/client" />

// Brings in the types for import.meta.env, which the app reads to find the
// base path it was published under. Vite generates this file in new
// projects; this one predates the first use of import.meta.
